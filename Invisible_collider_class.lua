Invisible_collider = Class(Game_object_base)
-- To create temporary objects with sprites attached to them or not that hit things (like the boomerang hat)

function Invisible_collider:_init(x, y, speed, direction, sprite, damage, collision_group, radius) 
	self.
